# requirements.txt
fastapi
uvicorn
numpy
aiohttp
python-dotenv # Needed by FastAPI for some functionalities, even if not directly using forms
# Add any other libraries your LLM integration would need (e.g., openai, google-generativeai)
